using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum DireccionSlice
{
    ARRIBA, ARRIBA_DERECHA, DERECHA, ABAJO_DERECHA, ABAJO, ABAJO_IZQUIERDA, IZQUIERDA, ARRIBA_IZQUIERDA
}

public class SliceDireccional : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
